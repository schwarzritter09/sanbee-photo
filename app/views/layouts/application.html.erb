<!DOCTYPE html>
<html>
<head>
  <title>SanbeePhoto</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="header">
  <%= link_to image_tag(asset_path('list.png'), { :border => '0', :alt => 'トップへ'}), articles_path %>
  <% if user_signed_in? %>
    <%= link_to image_tag(asset_path('logined.png'), { :border => '0', :alt => 'ログオフ'}), destroy_user_session_path %>
  <% else %>
    <%= link_to image_tag(asset_path('not_logined.png'), { :border => '0', :alt => 'ログイン'}), user_omniauth_authorize_path(:twitter) %>
  <% end %>
  <% if user_signed_in? %>
    <%= link_to image_tag(asset_path('new.png'), { :border => '0', :alt => '新規投稿'}), new_article_path %>
  <% end %>
  <%= link_to image_tag(asset_path('search.png'), { :border => '0', :alt => '検索'}), searcharticles_path %>
</div>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>

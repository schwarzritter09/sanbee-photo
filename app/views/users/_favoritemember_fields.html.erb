<div class="nested_fields">
  <%= f.hidden_field :id %>
  <%= f.select :member_id, options_for_select(Member.order_by_unit_kana.map{|m| [m.name, m.id]}, f.object.member_id) %>
  <%= f.link_to_remove '削除' %>
</div>
